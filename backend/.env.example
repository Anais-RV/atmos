# Configuración de variables de entorno para el backend de Atmos

# Información de la aplicación
APP_NAME=Atmos
APP_VERSION=1.0.0

# Configuración del servidor
HOST=0.0.0.0
PORT=8000

# ========================================
# CONFIGURACIÓN BÁSICA - ATMOS BACKEND
# ========================================

# Seguridad Django
# ⚠️ IMPORTANTE: Cambia SECRET_KEY en producción
SECRET_KEY=django-insecure-v5m=4q4)lopw%dfij@%szje+6nugh@gfdqy3d*d%u$xju5uf6o
DEBUG=True

# Base de datos (SQLite por defecto, PostgreSQL opcional)
# Para desarrollo, Django usa SQLite automáticamente (no necesitas configurar nada)
# Si necesitas PostgreSQL, descomenta y configura estas líneas:
# POSTGRES_DB=atmos_db
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=tu_password_aqui
# POSTGRES_HOST=localhost
# POSTGRES_PORT=5432

# CORS (permitir acceso desde frontend)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# ========================================
# CONFIGURACIÓN OPCIONAL (avanzado)
# ========================================

# API Keys externas (cuando las necesites)
# WEATHER_API_KEY=tu-api-key-aqui

# Email (cuando implementes notificaciones)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=tu-email@gmail.com
# SMTP_PASSWORD=tu-password

# Logging
# LOG_LEVEL=INFO
